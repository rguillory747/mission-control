[{'@/components/AgentCards': 'import { ActivityFeed'}, {'@/components/TaskBoard': 'import { MetricsBar'}, {'@/components/Header': 'import { Onboarding'}, {'@/components/RevenueTrackerV2': 'import { BuildQueue'}, {'@/components/TaskCreator': 'import { LeadTracker'}, {'@/components/ActionLog': 'import { useState', 'react";\n\ntype TabType = "dashboard" | "leads" | "actions': 'function TabButton({\n  label', 'label': 'string;\n  isActive: boolean;\n  onClick: () => void;'}, {'bg-emerald-500/20 text-emerald-500 border border-emerald-500/30': 'text-[#737373] hover:text-white hover:bg-[#1e1e1e] border border-transparent'}, {'useState<TabType>("dashboard': 'const [agents', 'data': ", err);\n        setError('Failed to load dashboard data", 'animate-pulse': 'tyle={{ width:', ' }} />\n          </div>\n          <div className="mt-4 text-sm text-[#737373]': 'Fetching data from Mission Control API...\n          </div>\n        </div>\n      </div>\n    );'}, {'mb-4': {'text-[#737373]': 'Agents: {agents.length'}, 'Tasks': {'Activities': {'leads': 'return (\n          <div className=', 'actions': 'return (\n          <div className=', 'overflow-auto': {'py-3': 'AgentCards agents={agents'}, 'Content': 'Activity Feed + Task Board */'}, 'min-h-0': 'TaskBoard tasks={tasks'}, 'Row': 'Metrics + Build Queue */'}, {'className="w-[70%]': 'MetricsBar metrics={metrics'}, {'className="w-[30%]': 'BuildQueue />\n              </div>\n            </div>\n\n            {/* Revenue Tracker */'}, [0, 'a0a0a] text-white flex flex-col">\n      {/* Header */}\n      <Header />\n\n      {/* Tab Navigation */}\n      <div className="flex gap-2 px-4 pt-3">\n        <TabButton\n          label="Dashboard"\n          isActive={activeTab === "dashboard"}\n          onClick={() => setActiveTab("dashboard")}\n        />\n        <TabButton\n          label="Leads"\n          isActive={activeTab === "leads"}\n          onClick={() => setActiveTab("leads")}\n        />\n        <TabButton\n          label="Actions"\n          isActive={activeTab === "actions"}\n          onClick={() => setActiveTab("actions")}\n        />\n      </div>\n\n      {/* Task Creator */}\n      <div className="px-4 py-3">\n        <TaskCreator />\n      </div>\n\n      {/* Main Content */}\n      {renderTabContent()}\n\n      {/* Data Status Footer */}\n      <div className=', 'px-4 py-2 text-xs text-[#737373] border-t border-[#1e1e1e]', 'Data: {agents.length} agents, {tasks.length} tasks, {activities.length} activities • \n        Last updated: {new Date().toLocaleTimeString()} • \n        <button \n          onClick={() => window.location.reload()}\n          className="ml-2 text-emerald-500 hover:text-emerald-400', 'Refresh\n        </button>\n      </div>\n    </div>\n  );\n}']]